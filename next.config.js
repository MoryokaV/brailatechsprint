/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cngmm.ro', 'imgur.com', 'i.imgur.com', 'archive.andreiusq.dev'],
  }
}

module.exports = nextConfig
